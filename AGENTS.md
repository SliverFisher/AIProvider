严格禁止兜底降级。

UI 门禁：搜索框必须复用项目现有统一搜索样式、图标位置和交互结构，禁止为单个页面另造不一致的搜索框视觉。

发布版本门禁：

- 首页左下角必须固定显示两行版本号，上面为“前端”，下面为“后端”，两套版本分别维护。
- 版本格式固定为 `年.月.日.当日发布序号.历史发布总序号`，例如 `2026.7.16.1.80`。
- 前端与后端的当日发布序号、历史发布总序号独立计数；只递增本次实际发布的一端，未发布的一端版本号不得变化。
- 每次生产发布前必须更新 `AIProvider-front/src/releaseVersion.js` 中对应端的版本；跨日后当日发布序号从 1 重新开始，历史发布总序号继续累加。
